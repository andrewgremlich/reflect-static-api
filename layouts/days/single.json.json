{
    "name": "{{ .Title }}",
    "description" : "{{ .Params.description }}",
    "link": "{{ .Permalink }}",
    "movements": [
        {{ range (where .Site.RegularPages ".Params.foreignid" "eq" .Params.id) }}
            "{{ . }}"
        {{ end }}
    ]
}